# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-04-25

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- **Adversarial review is expected.** Code review should be suspicious, not agreeable. Cite `file:line`, flag concrete findings, rank by severity (CRITICAL / HIGH / MEDIUM / LOW). No generic feedback.
- **Root cause first, always.** For any bug fix, state the root cause in one sentence ("X happens because Y") before proposing the change. Fix at the layer where the cause lives, not downstream of it.
- **Keep changes scoped.** Do exactly what was asked — nothing more, nothing less. No "while I was in here" refactors, no speculative abstractions, no preemptive cleanup.
- **Committing straight to main is fine.** (2026-07-13, user-confirmed) This is a solo repo with a push-to-main flow: commit directly on `main` and push when the user asks. Branches are still fine for larger/riskier work-in-progress, but don't create them by default or flag main-direct commits as a violation. Supersedes the "never work on main" rule in OPENWOLF.md's Branch Discipline for day-to-day changes.
- **No hacks.** No `# HACK`, no `# TODO: fix properly later`, no workaround comments, no try/except-pass, no hard-coded special cases to mask bad data. If the right fix is too large for scope, say so — don't ship a band-aid.
- (2026-07-12) Mined corpus extracts (docs/*_sequencing_corpus/) must NEVER be pushed to GitHub — they derive from purchased reference sequences. History was purged with git filter-repo + force-push on 2026-07-12 (and again 2026-07-13 to remove the vendor prefix from this file); docs/*_sequencing_corpus/ is gitignored on main; local copies are scrubbed of the vendor's package prefix (renamed to `Ref-`) and absolute paths. The real vendor prefix and show-folder path live only in the gitignored `.wolf/memory.md`. Apply the same treatment to any future prop-family extraction.
- (2026-07-12) Always ask the user which xlights_rgbeffects.xml (layout) file to use BEFORE generating any sequence — multiple show folders exist (the user's own show, the AI test show, per-package layouts) and assuming one risks targeting nonexistent models.

## Key Learnings

- **Project:** xlight-autosequencer — MP3 → xLights sequence generator pipeline. Python 3.11+, vamp/librosa/madmom analysis, Flask review UI, click CLI.
- **Review surface:** OPENWOLF.md now carries Branch Discipline and Code Review Discipline sections. `/review-diff` slash command runs the adversarial review against `git diff main...HEAD`.
- **Shared-infrastructure modules** (touching any of these requires cross-feature regression check and the full design-first gate — size is NOT a safe proxy for risk here): `src/analyzer/` (86 importers), `src/effects/` (54), `src/generator/` (44), `src/review/` (32), `src/themes/` (38). Measured 2026-04-24 by grepping callers outside each module.
- **Design-First Gate** (CLAUDE.md → "Design-First Gate" section): for any change beyond a single-file bug fix < ~30 lines with a stated root cause and no shared-module touch, produce a written design (OpenSpec change or inline plan) covering goal, approach, files touched, alternatives considered, regression surface (with grepped callers), and historical echoes from `.wolf/buglog.json` + cerebrum Do-Not-Repeat. Wait for user approval before editing project files. Use `/pre-mortem <change>` to stress-test the design before implementation. If the user says "just do it" — comply and note the override in the session summary.
- **Acceptance gate** (`xlight-evaluate gate`): unified pre-merge check covering analyzer + generator + UI suites. Exit codes (priority order, highest wins): `8` infra-failure (Playwright missing, corpus download failed, unknown error), `4` no-baseline (run `xlight-evaluate snapshot-analyzer` first), `6` regression detected, `0` pass. Modes: full (`gate`, all 5 fixtures + 5 UI flows, ~3-5 min), quick (`gate --quick`, 1 fixture + content UI flow only, ~90s), skip-ui (`gate --skip-ui`, no Playwright needed). The full analyzer suite requires the `.venv-vamp` sidecar (madmom + vamp); without it, the content UI flow auto-skips. **CI runs only the cheap tier** — unit tests, existing generator check, UI smoke flows (no content). The full gate is a **local pre-PR** step; install + runtime cost makes it impractical on fresh CI runners.
- **Validator confidence-write convention** (`src/analyzer/validator.py`): when a downstream pass populates `TimingMark.confidence` before `validate_hierarchy` runs (e.g. `selector.annotate_agreement_confidence` for L2 / L3 agreement), the validator must guard its own assignment with `if mark.confidence is None: mark.confidence = track_score`. Otherwise the bulk per-mark write at the end of each level's block silently clobbers the upstream value. Track-level scalars are still surfaced via `report['<level>']['score']`. This pattern applies to L1 / L4 / L0 marks too if a future change pre-populates them — same `if confidence is None` guard.
- **OpenSpec design artifacts are HTML, not markdown** (2026-05-14): each change's design lives at `openspec/changes/<name>/design.html` and links the shared stylesheet at `openspec/changes/_design.css` via `<link rel="stylesheet" href="../_design.css">`. The HTML format makes alternatives, regression surface, and iteration plans genuinely scannable instead of flattened into prose. `proposal.md`, `tasks.md`, and `specs/` deltas stay markdown — they are read by OpenSpec tooling and the `openspec-apply-change` skill. Reference example: `openspec/changes/tier-layering-policy/design.html`. Aesthetic adapted from https://thariqs.github.io/html-effectiveness/ (warm ivory + clay + slate palette). Do not re-inline CSS; always `<link>` the shared file. The convention is documented in CLAUDE.md → "Required contents of the design artifact".
- **Vamp plugins on the web/cloud container** (2026-06-01): the Claude Code web execution container is NOT built from `.devcontainer/Dockerfile` — it has no Vamp plugins (`VAMP_PATH` unset, `/usr/local/lib/vamp` empty). The Vamp Python *host* installs cleanly as a prebuilt PyPI wheel (`pip install vamphost`, currently 1.3.2) — no compile. The c4dm plugins build from source from their GitHub mirrors (`github.com/c4dm/{nnls-chroma,segmentino,qm-dsp,vamp-plugin-sdk}`); soundsoftware.ac.uk + the Qt installer-pack path are unreachable/headless-blocked here. nnls-chroma needs `libboost-dev` (build-time only, header `<boost/tokenizer.hpp>`) and apt `vamp-plugin-sdk` (headers + `/usr/lib/x86_64-linux-gnu/libvamp-sdk.a`). Link the SDK **statically** (`libvamp-sdk.a`, not `-lvamp-sdk`) so the `.so` is self-contained (ldd shows only libstdc++/libm/libc). segmentino is already self-contained (header-only armadillo, no LAPACK runtime dep). Built binaries are vendored at `vendor/vamp/linux-x86_64/` (see `vendor/vamp/README.md`); `scripts/install.sh` copies them to `$VAMP_DIR` as an offline fast path. Activate in a session with `export VAMP_PATH="$PWD/vendor/vamp/linux-x86_64"`. The project's algorithm classes are `ChordinoAlgorithm`/`NNLSChromaAlgorithm` (`vamp_harmony.py`) and `SegmentinoAlgorithm` (`vamp_segmentation.py`); segmentino is the L1 song-structure source consumed by `orchestrator.py` + `src/story/`. **All 7 required packs are now vendored** (2026-06-01): + `qm-vamp-plugins` (static `libqm-dsp.a` which bundles its own clapack/cblas — no system LAPACK), `bbc-vamp-plugins`, `beatroot-vamp`, `pyin`, `vamp-aubio`. Shared-build trick: compile the SDK's `PluginAdapter.o`/`RealTime.o`/`FFT.o` once and link them into each plugin to bake the SDK in. **vamp-aubio gotcha**: Ubuntu's `libaubio.so.5` drags in the entire ffmpeg/sndfile chain (~100 transitive libs); link the static `libaubio.a` + static `libfftw3f.a` instead (apt `libaubio-dev libfftw3-dev libsamplerate0-dev`) — the vamp plugin only uses aubio's DSP, not its file IO, so the linker drops the heavy objects and the `.so` becomes self-contained. waf.io is network-blocked so building aubio from source via waf is not an option here; use the apt static lib. Verified 17/17 plugin outputs + 36 plugin keys across 7 libraries on real audio.
- **Acceptance-gate analyzer drift is demucs, not plugin version** (2026-06-02): with real vamp active, `xlight-evaluate gate` analyzer suite fails (exit 6) on exactly two tracks — `qm_onsets_phase` (159) and `aubio_notes` (61), both `timing`. Proven NOT a plugin-version issue: `qm-vamp-plugins` built from tag `qm-vamp-plugins-v1.8.0` vs `master` are byte-identical (`cmp`) and produce identical onset output, both differing from the baseline. Root cause: these two run on demucs stems (`qm_onsets_phase`→drums, `aubio_notes`→vocals via `preferred_stem`), and demucs separation varies across torch/demucs versions + hardware → onset/note times shift ~100-200ms; index-based event comparison amplifies a single extra/missing onset into many drifts. All full-mix/rhythmic tracks (qm_bars, beatroot, segmentino, chordino, nnls) match within 50ms; generator PASSes. Documented in `tests/golden/analyzer/README.md`. Reconcile via `xlight-evaluate snapshot-analyzer` in-env, NOT by chasing demucs parity. Gate prereqs in this container: `pip install -e ".[dev]"` into `.venv-vamp` (numpy stays <2), Playwright absent so use `--skip-ui`.
- **xLights faceInfo semantics** (2026-07-12): `<faceInfo Type="NodeRange">` = node-mapped mouth shapes (dedicated singing props); `Type="Matrix"` = downloaded face-image sets (matrices/trees). Opening the Faces dialog on ANY prop leaves an empty NodeRange shell behind (all `Mouth-*` attrs blank, names like "Shadow"/"test") — singing-face detection must require ≥1 non-empty `Mouth-*` node range (bug-179). A model can hold several definitions (both "Female Face" and "Male Face" on each Singing Tree) — pick by name-token overlap with the model name.
- **Lyric timing sources differ in accuracy** (2026-07-12): the "Lyrics" phrase layer comes from syncedlyrics LRC timestamps (provider-supplied, often drifts vs the user's actual audio edition), while the word/phoneme layers come from WhisperX forced alignment on the audio itself (accurate). User-confirmed on Chattahoochee: words/phonemes/Faces/Text all correct, line layer "way off" but visually inert (nothing reads the phrase layer). If it ever matters, snap each line's start/end to its aligned words in `_build_lyric_layers` rather than trusting LRC times.
- **xLights Text effect can render a timing track** (2026-07-12): `E_CHOICE_Text_LyricTrack=<track name>` makes the Text effect display that track's labels as they fire — no per-word Text placements needed for word-synced lyrics on a matrix.
- **Windows dev host interpreters** (2026-07-12): pytest runs under the SYSTEM Python 3.13 (`python -m pytest`); the project `.venv` (3.12) holds flask/whisperx/torch/nltk/syncedlyrics but NOT pytest. There is no `.venv-vamp` on this host — whisperx imports fine in-process from `.venv`. `src/analyzer/phoneme_align.py` tries in-process first, then the `.venv-vamp` sidecar (checks both `bin/python` and `Scripts/python.exe`).
- **madmom on Python 3.11** (2026-06-02, bug-161): madmom 0.16.1 `processors.py` does `from collections import MutableSequence` (removed in 3.10 → `collections.abc`). Fixed with a shim mirroring the existing numpy-alias shim, in BOTH `vamp_runner.py` (module top) and `capabilities.py` (probe string + in-process branch — both needed or the probe reports madmom unavailable). Verified: `madmom_beats` now runs and matches baseline. Two leftover gotchas: (1) `madmom_downbeats` then failed with a SEPARATE numpy ragged-array `ValueError: inhomogeneous shape` — fixed in bug-162 via `_patch_downbeats_asarray()` in `madmom_beat.py` (module-scoped `np.asarray` proxy on `madmom.features.downbeats` that falls back to `dtype=object`, restoring pre-numpy-1.24 semantics madmom 0.16.1 relies on). (2) madmom needs `pkg_resources`, so `.venv-vamp` must keep `setuptools<81` + `packaging<25` (newer setuptools drops pkg_resources; the `packaging.markers` ModuleNotFoundError only appears in isolated test scripts that put repo root on sys.path[0], not the real gate subprocess).

- **Sequencing-technique corpus source** (2026-07-12): the ONLY reference corpus for mining hand-built sequencing techniques is the 12 vendor-prefixed professional packages (.xsqz/.zip) in the reference show folder — NOT the user's own mixed-quality sequences folder and NOT loose `.xsq` files in the reference folder (those are AI-generated outputs and would contaminate the corpus with our own patterns). Miner: `scripts/mine_prop_corpus.py <folder> --family <fam> --tokens <t1,t2> --only <vendor-prefix>`. The vendor prefix and both folder paths are recorded in the gitignored `.wolf/memory.md`, not here. Reference-corpus snowflake idiom: white Shockwave 66% / SingleStrand 28%, 93% white-base, 0.5-1 beat cadence, chorus-heavy.
- **Corpus color structure** (2026-07-13): duration-weighted palette mining across all elements of the 12 reference packages: pure white is 29-70% of lit time in EVERY show; song identity comes from 1-2 saturated accents (magenta/red/blue/purple/gold/amber families); per-effect palettes are 1-2 colors, almost never 4. Encoded as six white-first builtin themes (White Heat, Ice Crystal, Violet Night, Scarlet Ribbon, Solar Gold, Ember White) whose accent_palette leads with #FFFFFF (tiers 3+ read accent_palette; theme.palette feeds background tiers 1-2). `tests/unit/test_corpus_color_themes.py` also guards that every builtin theme's variants resolve.
- **Corpus Off-backdrop idiom** (2026-07-12): the reference packages tile qualifying sections with 12-15s Off blocks on the group element's layer index 1-2, beneath the beat bursts on layer 0 (snowflake Off 43/58 on L1; arch 69/75 on L1-2). Purpose: props render black between bursts instead of picking up whole-house bleed. Implemented as `PropFamilyRecipe.off_backdrop` (snowflake+arch True). xLights EffectLayer index 0 is the TOP layer; xsq_writer emits one EffectLayer per distinct `placement.layer` in sorted order.
- **Megatree corpus idiom** (2026-07-12): tree-proper placements (topper excluded — different prop family) are Shockwave 36% (same center-out burst preset as snowflakes, Blend_Edges/Cycles=1/Start_Width=5 at >96%), Spirals 18% (Count=1 96%, no grow/shrink, ~2-beat spins), On washes, Lightning accents; 93%+ solid single-color white. Recipe: Shockwave primary / Spirals alternate with mined preset via new `alt_parameter_overrides`. Mega tree effects live mostly on the *model* elements in the corpus; recipes fire on tier-6 06_PROP groups AND tier-8 HERO groups (recipe_for_group accepts both since 2026-07-12), so a solo mega tree (08_HERO_Mega_Tree) gets the recipe via the rotation path or the default placement path.
- **xLights first-color-only effects** (2026-07-12): On and Shockwave render only palette color 1; checking extra colors is invalid. `_FIRST_COLOR_ONLY_EFFECTS` in effect_placer trims accent palettes. Strobe uses all palette colors.
- **variation_seed is the global section index** (2026-07-12): `select_themes` assigns `variation_seed = base + section_index`, so in verse/chorus songs, choruses always share parity. Any "alternate on repeat" logic keyed on seed parity must halve first (`(seed // 2) % 2`) or it locks one branch in for the whole song (bug-182).
- **Per-song variety is seeded from the audio hash** (2026-07-14): `generator_runner._run_pipeline` sets `GenerationConfig.variation_seed = _derive_seed(audio_hash)` (int of first 8 hex chars; export passes song_id, which is sha256[:16] hex, as the hash). `select_themes`' `base_variation_seed` both offsets each section's variation_seed AND rotates the starting index inside `_pick_avoiding_adjacent`'s candidate pool — seed 0 preserves the legacy candidates[0] lineup (tests rely on that). Dashboard smart defaults mirror it via `_song_seed(song_id)` in `src/review/api/v1/analysis.py` so Theme-screen defaults match export-time auto-selection. Microscope is unaffected: it builds its own config with pinned variation_seed=42, but its metrics DID shift with this change (rotation now applies to seed 42) — repromote baselines after re-running `microscope sensitivity`.
- **Dashboard genre/occasion preferences** (2026-07-14): library-wide `preferences.genre` / `preferences.occasion` (validated enums mirroring builtin_themes.json vocab: any/pop/rock/classical, general/christmas/halloween) are read by smart theme defaults (analysis.py) and threaded into export generation (`export.py → generator_runner.run(genre=, occasion=)`). Frontend picker lives in TweaksPanel. Note plan.py treats genre "pop" as "unset" and may auto-infer classical from a minor key.

## Do-Not-Repeat

- **Restarting the dashboard in the devcontainer: never put `pkill -f <pattern>` in the same `docker exec sh -c` as the relaunch** — the shell's own command line contains the pattern, so pkill kills the launcher before the restart happens (bit us twice on 2026-07-13). Kill and relaunch in two separate `docker exec` calls: `docker exec xlight-dev pkill -f 'bin/xlight-review --dev'` then `docker exec -d xlight-dev sh -c 'cd /workspace && exec ~/.local/bin/xlight-review --dev --host 0.0.0.0 --port 5000 >/tmp/xlight-review.log 2>&1'`. Verify with GET /api/v1/manifest → backend_commit.
- **Never write the reference-corpus author's name or file prefix into anything tracked by git** — commit messages, code comments, docs, buglog, tests, or this file. (2026-07-13: a commit message named the author and had to be amended.) Say "the 12 hand-built reference packages" instead. The privacy rule covers the name itself, not just the corpus extracts; the name appears only in gitignored local files (memory.md, corpus dirs) and the auto-memory dir.
<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-04-19] Shipped changes that broke previously-working behavior because modified public symbols weren't audited for callers. Before finishing any diff, grep callers of every modified public function/method/CLI arg/schema field across `src/` and `tests/` and update or verify each one.
- [2026-04-19] Applied symptom fixes instead of root-cause fixes — swallowed exceptions with try/except-pass, added input-specific guard clauses, tuned magic numbers with no explanation, hard-coded special cases, post-processed bad output downstream. Always name the root cause first and fix it where it originates.
- [2026-04-19] Did more or less than what was asked — scope drift from unrelated refactors, or incomplete work that missed stated sub-requirements. Always restate the goal in one sentence before coding and verify every hunk of the diff advances that sentence.
- [2026-04-19] Committed work directly to `main` or bundled unrelated changes on an existing feature branch. Every new task starts with `git checkout -b <type>/<slug>` off `main`.
- [2026-04-25] Marked failing tests `pytest.mark.xfail` / `pytest.mark.skip` / CI `--ignore` instead of fixing them. Quarantine is a band-aid that decays into permanent dead code. When a test fails: fix the test, fix the code, or delete the test with a written rationale. The only acceptable quarantine is one paired with a `docs/known-broken-tests.md` entry stating diagnosis, remediation plan, and why it can't be fixed now.
- [2026-04-25] Patched `src.cli._variant_library_override` (the re-export) when consumers read `src.cli_old._variant_library_override` (the canonical). Re-exports do not propagate runtime mutations. Always patch the module the consumer imports from — find it by grepping where the symbol is *read*, not where it's *defined*.
- [2026-04-25] Tests inherited state from prior runs because module-level dicts (`_runs`, `_jobs`, `_library`) accumulate across test invocations. Any fixture that exercises a module holding state at module scope must reset that state explicitly before yielding the app/client.
- [2026-05-31] Diagnosed a non-existent "energy→brightness inversion" (claimed r=−0.59) by feeding the generator **un-normalized** per-section energy scores. The real pipeline normalizes section energy across the song (`src/story/builder.py:550`, min→0/max→100). With normalized scores the inversion vanished (Maple Leaf Rag r=+0.89, Nostalgic Piano r=−0.17). Lesson: before measuring/“fixing” generator behavior off a hand-built story or proxy analysis, replicate the real pipeline's transforms (esp. energy normalization) on the input — otherwise the measurement is an artifact. Confirm a premise on representative input *before* writing a design. The `energy-brightness-gradient` change was withdrawn as a result.
- [2026-05-31] The librosa-only path produces 0 hierarchy `sections` (segmentino/QM are vamp plugins). Generating without a `--story` then silently yields a timing-only (unlit) `.xsq` (see buglog bug-159). For render/measurement work in a librosa-only env, supply a story; for production-grade energy+sections, use the real vamp/madmom analyzer, not the proxy.
- [2026-07-11] On Windows, `print()`/`click.echo()` with unicode glyphs (`✓`/`✗`) crashes with `UnicodeEncodeError` because stdout/stderr default to the `cp1252` codec unless the console is UTF-8. Fixed at the root by reconfiguring `sys.stdout`/`sys.stderr` to UTF-8 once in the CLI entrypoint (`src/cli/__init__.py:main()`) rather than swapping glyphs at each call site — the glyphs appear in `src/analyzer/orchestrator.py`, `src/cli_old.py`, and `src/analyzer/progress.py`, and patching only one site leaves the others live. Also: fixing this at the entrypoint (not inside `src/analyzer/`) avoided the shared-module design gate since `src/cli/` isn't in CLAUDE.md's gated list.
- [2026-07-11] `code.soundsoftware.ac.uk` (source of pre-built amd64 Vamp plugin binaries in `.devcontainer/Dockerfile`) is permanently offline — DNS resolves but TCP connection times out entirely, not a transient slowness/rate-limit issue. Do not spend time bumping retries/timeouts against this host again. The fix that worked: removed the amd64-vs-arm64 branch in the Vamp Plugins Dockerfile step and always build all 9 plugins from source via their upstream GitHub repos (`c4dm/*`, `bbc/*`, `aubio/*`, `cannam/*`) — the arm64 from-source path already existed and covers every plugin the amd64 branch used to download. The GitHub-hosted "Vamp Plugin Pack" installer (`github.com/vamp-plugins/vamp-plugin-pack`) is NOT a usable shortcut: it's a Qt GUI app with the actual `.so` files not present as static files in the AppImage, and no unattended/silent-install CLI flag was found by string-searching the binary.
- [2026-07-11] `src/cli_old.py` and `src/cli/*.py` can define commands with the SAME name (e.g. both had `@cli.command("review")`). `src/cli/extras.py` only re-registers a `cli_old.py` command `if cmd_name not in cli.commands`, and the new split modules (`analyze`, `library`, `review`, `scoring`) are imported before `extras`, so any command already rewritten in `src/cli/` silently shadows the identically-named `cli_old.py` version — the old one becomes dead code that still runs at import time (decorators fire) but is never dispatched. Before editing any command in `cli_old.py`, grep `src/cli/*.py` for the same `@cli.command("name")` first — if it exists there, that's the live one.
- [2026-07-11] `~/.xlight/library.json`'s `source_paths` are stored as literal OS-native absolute paths (e.g. `C:\Users\daryl\...`) with NO cross-platform translation — unlike `src/paths.py`'s `get_show_dir()`/`resolve_show_path()`, which only handles the xLights *show directory*. This means a library entry created on Windows can never resolve inside a Linux devcontainer (or vice versa) without a real design change to the library storage layer. Don't try to quick-patch around this with mounts/symlinks — the path strings themselves are the blocker, not the filesystem layout. Flagged as a real gap, not yet fixed; would need a proper design (touches `src/review/`, gated module) before attempting.
- [2026-07-11] The `H:\Github\xlight-autosequencer\.devcontainer` folder is the "Claude Code Sandbox" devcontainer, and per the recent "Linux analyzer enablement" work (`d3210a8`) it also doubles as the full vamp/madmom/demucs analyzer environment — `XLIGHT_VENV_VAMP=/workspace/.venv-vamp/bin/python` symlinks to the container's system `python3`. When `docker build`-ing this Dockerfile manually (not via VS Code's devcontainer CLI), the build context must be `.devcontainer/` itself, not the repo root — `devcontainer.json` has no explicit `"context"` override so its implicit context is the folder containing it. `docker build -f .devcontainer/Dockerfile .devcontainer` is correct; `docker build -f .devcontainer/Dockerfile .` fails on the `COPY init-firewall.sh` / `COPY xlights-check.sh xlights-render.sh` steps (files not found at repo root).

- [2026-07-12] On this Windows host, piping Python source into `python - <<'EOF'` via the Bash tool strips one level of backslash escapes — string literals with doubled backslashes arrive single-backslashed (SyntaxWarning, silent no-match in replaces). Avoid backslash-laden literals in heredoc Python; use plain tokens without backslashes, raw strings from `sys.argv`, or write a real script file.
- [2026-07-12] Gated song-scoped vocal placements (Faces/Text) on `assignments[0]` existing — a librosa-only analysis yields 0 sections → 0 assignments, silently dropping them (bug-178, same family as bug-159). Anything that must survive a 0-section analysis must ride on the plan itself (`SequencePlan.vocal_effects`), never on section assignments.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-04-19] Added adversarial review discipline to OpenWolf (OPENWOLF.md sections + `/review-diff` command). Chosen over editing built-in `review`/`security-review` skills because those live outside the repo and can't be versioned; chosen over tightening `/speckit.analyze` because the pain is post-implementation (diffs, regressions) not pre-implementation (spec quality).

